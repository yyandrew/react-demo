# Demo by create-react-app

## start app

```
npm install -g create-react-app # if your already installed please skip this step
git clone https://github.com/yyandrew/react-demo.git
# start express
cd react-demo/backend
npm i
npm start
# start react server
cd react-demo
npm i
npm start
```
